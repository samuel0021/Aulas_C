#include <stdio.h>
#include <conio.h>
#include <locale.h>

int main(){
    setlocale(LC_ALL, "");

    int numeros[10] = {1, 2, 3, 4, 4, 5, 2, 6, 5};
    int length = sizeof(numeros) / sizeof(numeros[0]);
    
    
}
